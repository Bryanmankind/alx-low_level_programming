#include<stdio.h>

/**
 * main - print the digit number of base 10
 *
 * return always 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d",i);
	}
	putchr('\n');
	return (0);
}
